# Database
DB_HOST=localhost
DB_NAME=qbo_pco_sync
DB_USER=qbopcosync
DB_PASS=changeme
DB_CHARSET=utf8mb4

# QuickBooks
QBO_CLIENT_ID=your_qbo_client_id
QBO_CLIENT_SECRET=your_qbo_client_secret
QBO_REDIRECT_URI=https://your-host/qbo-pco-sync/public/oauth-callback.php
QBO_BASE_URL=https://quickbooks.api.intuit.com
QBO_ENVIRONMENT=production

# Planning Center
PCO_APP_ID=your_pco_app_id
PCO_SECRET=your_pco_secret
PCO_BASE_URL=https://api.planningcenteronline.com

# Webhook secrets
PCO_WEBHOOK_BATCH_CREATED=replace_me
PCO_WEBHOOK_BATCH_UPDATED=replace_me
PCO_WEBHOOK_DONATION_CREATED=replace_me
PCO_WEBHOOK_DONATION_UPDATED=replace_me

# App
APP_BASE_URL=https://your-host/qbo-pco-sync/public
APP_NOTIFICATION_EMAIL=you@example.com
MAIL_FROM=you@example.com

# SMTP settings for outbound email (errors/notifications)
MAIL_SMTP_HOST=smtp.dreamhost.com
MAIL_SMTP_PORT=587
MAIL_SMTP_USER=
MAIL_SMTP_PASS=
# tls for port 587, ssl for 465
MAIL_SMTP_ENCRYPTION=tls
